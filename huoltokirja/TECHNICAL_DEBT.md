# Technical Debt

- Form validation is basic and can be centralized into reusable validators.
- Error localization and user-facing error messages should be expanded.
- Router could evolve to a multi-tab shell for larger UX flows.
- Migration strategy is defined, but migration v2+ scripts are still placeholders.
